let Country = "india";
let continent = "Asia";
let Population = 8;
let language = "hindi";

const discription = `${Country} is in ${continent}, and its ${Population} milloin people speak ${language}`;
console.log(discription);
